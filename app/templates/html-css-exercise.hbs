<div class="panel panel-default">
    <div class="panel-heading">
        <strong>Ember Exercises</strong>
    </div>
    <div class="panel-body" id="animations-exercise">
        <h2>Instructions:</h2>
        <p>Modify the existing <code>shrinking-box</code> component to meet the following criteria:</p>
        <ol>
            <li>
                Update the box to be 360px wide and 360px tall, and a background color of rgb(99, 99, 99).
            </li>
            <li>
                When the box is clicked, toggle the <code>isSmall</code> property and use CSS properties to change the size to 180px wide and 180px tall, the background to rgb(12, 89, 100), and font color to white.
            </li>
            <li>
                Use CSS transitions to animate these property changes over 500ms.
            </li>
            <li>
                Update <code>shrinking-box.hbs</code> to render the <code>smallMessage</code> property when the box is small.
            </li>
            <li>
                Update <code>shrinking-box.hbs</code> to render the <code>bigMessage</code> property when the box is not small.
            </li>
            <li>
                Use flexbox properties to center the messages both vertically and horizontally.
            </li>
        </ol>
        <h2>Results:</h2>
        <div>
            {{shrinking-box bigMessage="I'm big!" smallMessage="I'm small!"}}
        </div>
    </div>
</div>